# MCP server (requires Python 3.10+)
mcp>=1.2.0

# Google Drive (OAuth user) + Sheets (service account)
google-api-python-client>=2.120.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
gspread>=6.0.0

# PDF rendering — pure Python, no system libraries required
fpdf2>=2.7.8
